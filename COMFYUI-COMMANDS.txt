## COMFYUI COMMANDS

# Kill all ComfyUI instances
pkill -f "python main.py" && sleep 2 && echo "All ComfyUI instances killed"

# Start ComfyUI with CORS enabled
python main.py --listen 0.0.0.0 --port 8188 --enable-cors-header

# Start ComfyUI with additional options
python main.py --listen 0.0.0.0 --port 8188 --enable-cors-header --preview-method auto

# Stop current ComfyUI (Ctrl+C)
# Then restart with low VRAM mode
python main.py --listen 0.0.0.0 --port 8188 --enable-cors-header --lowvram

# Current Status: 🎉 HYBRID GPU/CPU APPROACH WORKING - NO MORE CRASHES!
# WebSocket handler modified directly in server.py for WhisperX integration
# Server now processes whisperx_audio_chunk messages with WhisperX and sends back whisperx_transcription_result
# Audio processing supports multiple formats (float32, int16, int32, uint8)
# ✅ NODE LOADING: SimpleLiveTranscription node now loads successfully
# ✅ NODE REGISTRATION: Global node instance created and registered automatically
# ✅ WhisperX integration working: Messages being processed successfully
# ✅ Audio buffering: 1.5-second buffer for faster response (was 3.0s → 1.5s → 1.0s → 2.0s → 1.5s)
# ✅ WebSocket connections: Being established successfully
# ✅ Audio processing: Working - processing 1.5-second audio chunks
# ✅ Audio level monitoring: Max/RMS levels being logged
# ✅ Transcription result processing: Fixed to handle tuple format with generator
# ✅ WebSocket send methods: Fixed to use send_str() for text data
# ✅ Node auto-registration: Node registers itself when instantiated
# ✅ Silence detection: Skipping audio with RMS < 0.002 (optimized threshold)
# ✅ False positive filtering: Enhanced filtering for "thank you" phrases
# ✅ HYBRID GPU/CPU: Model loads on GPU, transcription processes on CPU
# ✅ NO MORE CRASHES: cuDNN crashes eliminated with CPU transcription
# ✅ SPEED OPTIMIZATIONS: Greedy decoding, optimized parameters for faster processing
# 🔧 FIXED: Critical indentation error preventing node loading
# 🔧 FIXED: Critical indentation issue in _process_audio_buffer method
# 🔧 FIXED: WebSocket send methods using correct send_str() for text data
# 🔧 FIXED: Node registration by auto-registering in __init__
# 🔧 OPTIMIZED: Speech detection parameters for sentence grouping
# 🔧 ENHANCED: Generator consumption debugging with detailed segment logging
# 🔧 FIXED: Critical bug - Segment objects now properly processed (was using .get() instead of .text)
# 🔧 OPTIMIZED: False positive filtering (removes "thank you" generation)
# 🔧 OPTIMIZED: Buffer duration set to 2.0s for better sentence grouping
# 🔧 OPTIMIZED: Silence detection threshold lowered to 0.003 for better grouping
# 🔧 OPTIMIZED: Enhanced false positive detection with better phrase matching
# 🔧 OPTIMIZED: WhisperX parameters for sentence continuity (condition_on_previous_text=True)
# 🔧 REVERTED: CPU mode for stability (avoiding cuDNN crashes)
# 🎯 READY: Stable configuration optimized for sentence grouping - test transcription functionality